name: Deploy Production (Pondok Informatika)

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      - name: Build for Production
        run: npm run build:pondok

      - name: Install sshpass
        run: sudo apt-get install -y sshpass

      - name: Deploy to VPS
        env:
          VPS_PASSWORD: ${{ secrets.VPS_PASSWORD }}
        run: |
          if [ -z "$VPS_PASSWORD" ]; then
            echo "Error: VPS_PASSWORD secret is missing in GitHub Settings."
            exit 1
          fi
          # Using sshpass with rsync
          # Added -o StrictHostKeyChecking=no to prevent interactive prompt
          sshpass -p "$VPS_PASSWORD" rsync -avz --delete -e "ssh -o StrictHostKeyChecking=no" dist/ pi@210.79.191.137:/var/www/pondokinformatika.id/
